# shaders-tests

Test repository for writing and testing GLSL shaders in Three.js with Vite.

## Scripts

```sh
npm install
npm run dev
npm run build
```

Shader entry points live in `src/shaders/vertex.glsl` and `src/shaders/fragment.glsl`.

